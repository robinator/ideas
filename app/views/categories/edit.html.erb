<%= render 'side_menu'%>
<h2>Editing category</h2>

<% form_for(@category) do |f| %>
  <%= f.error_messages %>
	<p>
		<%= f.label 'name' %><br/>
		<%= f.text_field :name %>
	</p>
  <p>
    <%= f.submit 'Update' %> <%= link_to 'cancel', @category %>
  </p>
<% end %>